on:
  push:
    branch: main
  pull_request:
    branch: main

jobs:
  spectral:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: curl https://italia.github.io/api-oas-checker/spectral.yml > .spectral.yml

      - uses: stoplightio/spectral-action@v0.8.8
